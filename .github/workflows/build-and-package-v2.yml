- name: Debug full build tree
  run: |
    echo "==== FULL BUILD STRUCTURE ===="
    find build -type f || echo "No files in build directory"

- name: Upload all build output (force capture)
  uses: actions/upload-artifact@v4
  with:
    name: xpressbuy247-suite
    path: |
      build
      build/**/*.zip
